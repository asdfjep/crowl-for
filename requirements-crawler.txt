# Crawler service (crawler/hs-news-service) runtime dependencies.
# Loose pins: the analyzer only exercises the import graph + HTTP fetch path
# (health checks, one-shot crawl), not the admin/browser features.
sqlalchemy>=2.0
aiohttp>=3.9
beautifulsoup4>=4.12
lxml>=5.0
requests>=2.31
python-dotenv>=1.0
jinja2>=3.1
itsdangerous>=2.1
python-multipart>=0.0.9
playwright>=1.40